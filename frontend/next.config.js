/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If deploying to GitHub Pages, uncomment:
  // output: 'export',
  // basePath: '/synctrade-static',
  // trailingSlash: true,
  
  // For Vercel deployment (recommended), keep default settings
}

module.exports = nextConfig

